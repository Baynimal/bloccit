<h3 id="post-<%= @post.id %>-vote">
  <%= pluralize(@post.points, 'point') %>
  <div>
    <small>
      <%= pluralize(@post.up_votes, 'up vote') %>
      <%= pluralize(@post.down_votes, 'down vote') %>
    </small>
  </div>
</h3>
